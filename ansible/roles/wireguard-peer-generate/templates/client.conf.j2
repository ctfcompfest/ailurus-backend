[Interface]
PrivateKey = {{wgclient_privkey}}
Address = {{wgclient_ip}}/32
DNS = {{dns | default([]) | join(',')}}

[Peer]
PublicKey = {{wgserver_pubkey}}
AllowedIPs = {{allowed_ips}}
Endpoint = {{endpoint.ip}}:{{endpoint.port}}
PersistentKeepalive = 20
